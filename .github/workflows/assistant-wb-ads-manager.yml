name: assistant_wb_ads_manager

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'Режим запуска'
        required: true
        type: choice
        default: preview
        options:
          - preview
          - run
      confirm_live:
        description: 'Подтвердить боевой запуск'
        required: false
        type: boolean
        default: false
      as_of_date:
        description: 'Дата запуска YYYY-MM-DD (необязательно)'
        required: false
        type: string
        default: ''
  schedule:
    - cron: '15 3 * * *'

concurrency:
  group: assistant-wb-ads-manager-topface
  cancel-in-progress: false

jobs:
  daily_preview:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
    env:
      YC_ACCESS_KEY_ID: ${{ secrets.YC_ACCESS_KEY_ID }}
      YC_SECRET_ACCESS_KEY: ${{ secrets.YC_SECRET_ACCESS_KEY }}
      YC_BUCKET_NAME: ${{ secrets.YC_BUCKET_NAME }}
      WB_PROMO_KEY_TOPFACE: ${{ secrets.WB_PROMO_KEY_TOPFACE }}
      PYTHONUNBUFFERED: '1'
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install boto3 pandas openpyxl requests botocore

      - name: Run preview
        run: |
          python assistant_wb_ads_manager.py preview

  manual_run:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
    env:
      YC_ACCESS_KEY_ID: ${{ secrets.YC_ACCESS_KEY_ID }}
      YC_SECRET_ACCESS_KEY: ${{ secrets.YC_SECRET_ACCESS_KEY }}
      YC_BUCKET_NAME: ${{ secrets.YC_BUCKET_NAME }}
      WB_PROMO_KEY_TOPFACE: ${{ secrets.WB_PROMO_KEY_TOPFACE }}
      PYTHONUNBUFFERED: '1'
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install boto3 pandas openpyxl requests botocore

      - name: Validate live confirmation
        if: ${{ inputs.mode == 'run' && inputs.confirm_live != true }}
        run: |
          echo 'Для mode=run нужно поставить confirm_live=true'
          exit 1

      - name: Run preview without date
        if: ${{ inputs.mode == 'preview' && inputs.as_of_date == '' }}
        run: |
          python assistant_wb_ads_manager.py preview

      - name: Run preview with date
        if: ${{ inputs.mode == 'preview' && inputs.as_of_date != '' }}
        run: |
          python assistant_wb_ads_manager.py preview --as-of-date "${{ inputs.as_of_date }}"

      - name: Run live without date
        if: ${{ inputs.mode == 'run' && inputs.as_of_date == '' }}
        run: |
          python assistant_wb_ads_manager.py run --apply-shades

      - name: Run live with date
        if: ${{ inputs.mode == 'run' && inputs.as_of_date != '' }}
        run: |
          python assistant_wb_ads_manager.py run --apply-shades --as-of-date "${{ inputs.as_of_date }}"
